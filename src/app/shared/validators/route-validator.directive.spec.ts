import { RouteValidatorDirective } from './route-validator.directive';

describe('RouteValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
